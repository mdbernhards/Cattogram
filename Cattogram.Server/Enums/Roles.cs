namespace Cattogram.Server.Database.Enums
{
    public enum Roles
    {
        // User role
        User = 0,

        // Moderatore role
        Moderator = 1,

        // Administrator role
        Admin = 2,
    }
}